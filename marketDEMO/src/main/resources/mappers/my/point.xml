<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="kr.co.team3.mapper.my.PointMapper">
    <select id="selectRecent5History" resultType="kr.co.team3.dto.my.PointDTO">
        SELECT po.PO_ORDERDATE, po.PO_STATE, ph.*
        FROM POINT_HISTORY ph
        JOIN PRODUCT_ORDER po ON ph.UH_PO_NO = po.PO_NO
        WHERE ph.UH_U_ID = #{uId}
        ORDER BY po.PO_ORDERDATE DESC
    </select>
    <select id="selectPoints" resultType="int">
        SELECT U_POINT FROM U_USER
        WHERE U_ID = #{uId}
    </select>
</mapper>